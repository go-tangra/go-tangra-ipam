# Configuration for IPAM service OpenAPI generation
version: v2

clean: false

managed:
  enabled: true

  disable:
    - module: buf.build/googleapis/googleapis
    - module: buf.build/bufbuild/protovalidate
    - module: buf.build/kratos/apis

  override:
    - file_option: go_package
      path: ipam/service/v1
      value: github.com/go-tangra/go-tangra-ipam/gen/go/ipam/service/v1;ipampb

inputs:
  - directory: protos
    paths:
      - protos/ipam/service/v1

plugins:
  # generate openapi v3 yaml doc
  - local: protoc-gen-openapi
    out: cmd/server/assets
    opt:
      - naming=json
      - depth=2
      - default_response=false
      - enum_type=string
      - output_mode=merged
      - fq_schema_naming=false
